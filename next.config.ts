import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // App Router + generateStaticParams above already gives us full SSG
  // for all 1430 Ang routes. Nothing extra needed here for the prototype.
};

export default nextConfig;
